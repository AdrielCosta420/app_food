import 'package:flutter/material.dart';
import './cadastro_controller.dart';

class CadastroPage extends StatelessWidget {
  const CadastroPage({
    Key? key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('CadastroPage'),
      ),
      body: Container(),
    );
  }
}
