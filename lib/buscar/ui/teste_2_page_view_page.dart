import 'package:flutter/material.dart';

class Teste2PageViewPage extends StatelessWidget {
  const Teste2PageViewPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
